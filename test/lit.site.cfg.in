@LIT_SITE_CFG_IN_HEADER@

import sys

config.mutang_obj_root  = "@MUTANG_BINARY_DIR@"
config.mutang_tools_dir = "@MUTANG_TOOLS_DIR@"

# Let the main config do the real work.
lit_config.load_config(config, "@MUTANG_SOURCE_DIR@/test/lit.cfg")

